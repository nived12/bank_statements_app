<div>
  <%= render 'common/section_header', title: t('savings.title') do %>
    <%= link_to new_saving_path, class: "btn-primary flex items-center space-x-2", data: { turbo_frame: "_top" } do %>
      <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
      </svg>
      <%= t('savings.add_savings') %>
    <% end %>
  <% end %>

  <%= render 'common/planning/tabs', planning_type: 'savings' %>

  <section class="w-full flex justify-center">
    <div class="container p-8">
      <% if @savings.any? %>
        <div id="savings-list" class="grid grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 gap-6">
          <% @savings.each do |saving| %>
            <%= render 'common/planning/card', item: saving %>
          <% end %>
      <% else %>
        <%= render 'common/empty_section',
              title: t('savings.no_savings_yet'),
              description: t('savings.create_first_description'),
              button_text: t('savings.create_first'),
              button_path: new_saving_path %>
      <% end %>
    </div>
  </section>
</div>