<div data-controller="debt-new">

  <!-- Header section -->
  <%= render 'common/section_header', title: t('debts.new_debt'), back_path: debts_path %>

  <!-- Form -->
  <div class="w-full flex justify-center">
    <div class="container p-8">
      <%= render 'form', debt: Debt.new %>
    </div>
  </div>
</div>