# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: recurring.spec.ts >> authenticated user >> detail view (show.html.erb) >> renders the canonical detail layout for a newly created series
- Location: e2e/tests/recurring.spec.ts:61:9

# Error details

```
TimeoutError: page.waitForURL: Timeout 30000ms exceeded.
=========================== logs ===========================
waiting for navigation until "domcontentloaded"
  navigated to "http://127.0.0.1:3001/recurring/new"
============================================================
```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e3]:
    - complementary [ref=e4]:
      - generic [ref=e5]:
        - link "Vittio BETA" [ref=e7] [cursor=pointer]:
          - /url: /
          - generic [ref=e8]:
            - img "Vittio" [ref=e11]
            - generic [ref=e12]: BETA
        - navigation [ref=e13]:
          - link "Panel" [ref=e14] [cursor=pointer]:
            - /url: /dashboard
            - img [ref=e15]
            - text: Panel
          - link "Cuentas" [ref=e20] [cursor=pointer]:
            - /url: /bank_accounts
            - img [ref=e21]
            - text: Cuentas
          - link "Categorías" [ref=e23] [cursor=pointer]:
            - /url: /categories
            - img [ref=e24]
            - text: Categorías
          - link "Transacciones" [ref=e26] [cursor=pointer]:
            - /url: /transactions
            - img [ref=e27]
            - text: Transacciones
          - link "Estados Cuenta" [ref=e30] [cursor=pointer]:
            - /url: /statement_files
            - img [ref=e31]
            - text: Estados Cuenta
          - generic [ref=e34]:
            - heading "Metas" [level=3] [ref=e36]
            - link "Metas" [ref=e37] [cursor=pointer]:
              - /url: /goals
              - img [ref=e38]
              - text: Metas
            - link "Ahorros" [ref=e42] [cursor=pointer]:
              - /url: /savings
              - img [ref=e43]
              - text: Ahorros
            - link "Deudas" [ref=e46] [cursor=pointer]:
              - /url: /debts
              - img [ref=e47]
              - text: Deudas
        - generic [ref=e50]:
          - link "Vittbot" [ref=e51] [cursor=pointer]:
            - /url: /assistant
            - img [ref=e52]
            - text: Vittbot
          - link "Suscripción" [ref=e55] [cursor=pointer]:
            - /url: /subscription
            - img [ref=e56]
            - text: Suscripción
          - link "Ajustes" [ref=e58] [cursor=pointer]:
            - /url: /settings
            - img [ref=e59]
            - text: Ajustes
          - link "Playwright E2E Playwright E2E Editar perfil" [ref=e62] [cursor=pointer]:
            - /url: /profile
            - img "Playwright E2E" [ref=e64]
            - generic [ref=e65]:
              - paragraph [ref=e66]: Playwright E2E
              - paragraph [ref=e67]: Editar perfil
          - generic [ref=e68]:
            - button "ES" [ref=e71] [cursor=pointer]:
              - generic [ref=e72]: ES
              - img [ref=e73]
            - generic [ref=e75]:
              - button "Cambiar modo oscuro" [ref=e77]:
                - img [ref=e79]
              - button "Cerrar sesión" [ref=e82]:
                - img [ref=e83]
    - main [ref=e87]:
      - generic [ref=e91]:
        - img [ref=e93]
        - heading "Error del Servidor" [level=1] [ref=e95]
        - paragraph [ref=e96]: Lo sentimos, ocurrió un error interno. Estamos trabajando para solucionarlo.
        - generic [ref=e97]:
          - link "Volver al Panel" [ref=e98] [cursor=pointer]:
            - /url: /dashboard
            - img [ref=e99]
            - text: Volver al Panel
          - button "Regresar" [ref=e101]:
            - img [ref=e102]
            - text: Regresar
        - paragraph [ref=e105]: Si el problema persiste, por favor contacta al soporte técnico.
  - button "Vittbot" [ref=e106]:
    - img [ref=e107]
    - generic [ref=e110]: Vittbot
```

# Test source

```ts
  1   | import { expect, test } from "@playwright/test";
  2   | import { desktopFormShell, desktopRecurringShow, desktopRecurringTab } from "../helpers/desktop";
  3   | 
  4   | test("unauthenticated visit redirects to sign in", async ({ page }) => {
  5   |   await page.goto("/recurring");
  6   |   await expect(page).toHaveURL(/\/session\/new/);
  7   | });
  8   | 
  9   | test.describe("authenticated user", () => {
  10  |   test.use({ storageState: "e2e/.auth/user.json" });
  11  | 
  12  |   test("renders the recurring index page or redirects when subscription gate denies", async ({ page }) => {
  13  |     await page.goto("/recurring");
  14  |     // Either user has access (sees the recurring page) or is redirected to /pricing
  15  |     const url = page.url();
  16  |     if (/\/pricing/.test(url)) {
  17  |       test.skip(true, "subscription gate denied user — pricing page reached");
  18  |       return;
  19  |     }
  20  |     await expect(page.getByRole("heading", { name: /Recurr(ing|entes)/i }).first()).toBeVisible();
  21  |     await expect(page.getByText(/Monthly total|Total mensual/i).first()).toBeVisible();
  22  |     await expect(page.getByText(/Annual total|Total anual/i).first()).toBeVisible();
  23  |   });
  24  | 
  25  |   test("'New recurring' link routes to the form", async ({ page }) => {
  26  |     await page.goto("/recurring");
  27  |     if (/\/pricing/.test(page.url())) {
  28  |       test.skip(true, "subscription gate denied");
  29  |       return;
  30  |     }
  31  |     await page.getByRole("link", { name: /New recurring|Nuevo recurrente/i }).first().click();
  32  |     await expect(page).toHaveURL(/\/recurring\/new/);
  33  |     const desktopForm = desktopFormShell(page, "recurring-series-form");
  34  |     await expect(desktopForm.locator('input[name="recurring_series[name]"]')).toBeVisible();
  35  |     await expect(desktopForm.locator('input[name="recurring_series[expected_amount]"]')).toBeVisible();
  36  |   });
  37  | 
  38  |   test("Transactions page shows 'Recurrentes' tab and navigates correctly", async ({ page }) => {
  39  |     await page.goto("/transactions");
  40  |     if (/\/pricing/.test(page.url())) {
  41  |       test.skip(true, "subscription gate denied");
  42  |       return;
  43  |     }
  44  |     const recurringTab = desktopRecurringTab(page);
  45  |     await expect(recurringTab).toBeVisible();
  46  |     await Promise.all([
  47  |       page.waitForURL(/\/recurring($|\?)/, { waitUntil: "domcontentloaded", timeout: 30_000 }),
  48  |       recurringTab.click(),
  49  |     ]);
  50  |     // Active state of the parent sidebar should still cover the recurring controller
  51  |     await expect(page.getByText(/Monthly total|Total mensual/i).first()).toBeVisible();
  52  |   });
  53  | 
  54  |   test.describe("detail view (show.html.erb)", () => {
  55  |     // Creates a fresh series via the new form so the spec is self-contained.
  56  |     // The series name is timestamped to avoid uniqueness collisions across runs.
  57  |     // new.html.erb renders the _form partial twice (mobile + desktop wrappers).
  58  |     // We scope to the visible desktop form so .fill()/.click() hit the right copy.
  59  |     const seriesName = `E2E Detail ${Date.now()}`;
  60  | 
  61  |     test("renders the canonical detail layout for a newly created series", async ({ page }) => {
  62  |       await page.goto("/recurring/new");
  63  |       if (/\/pricing/.test(page.url())) {
  64  |         test.skip(true, "subscription gate denied");
  65  |         return;
  66  |       }
  67  | 
  68  |       const desktopForm = desktopFormShell(page, "recurring-series-form");
  69  |       await desktopForm.locator('input[name="recurring_series[name]"]').fill(seriesName);
  70  |       await desktopForm.locator('input[name="recurring_series[expected_amount]"]').fill("99.00");
  71  |       await Promise.all([
> 72  |         page.waitForURL(/\/recurring\/\d+$/, { waitUntil: "domcontentloaded", timeout: 30_000 }),
      |              ^ TimeoutError: page.waitForURL: Timeout 30000ms exceeded.
  73  |         desktopForm.getByRole("button", { name: /Create recurring|Crear recurrente/i }).click(),
  74  |       ]);
  75  | 
  76  |       // Should land on the show page.
  77  |       await expect(page).toHaveURL(/\/recurring\/\d+$/);
  78  | 
  79  |       // show.html.erb renders mobile-first then desktop. At the default Chromium
  80  |       // viewport (>=768px) only the desktop copy is visible — pick it with .last().
  81  |       await expect(page.getByRole("heading", { name: seriesName }).last()).toBeVisible();
  82  |       await expect(page.getByText(/Active|Activa/i).last()).toBeVisible();
  83  |       await expect(page.getByText(/Fixed expense|Gasto fijo/i).last()).toBeVisible();
  84  | 
  85  |       // Summary card stats.
  86  |       await expect(page.getByText(/Monthly total|Total mensual/i).last()).toBeVisible();
  87  |       await expect(page.getByText(/Annual total|Total anual/i).last()).toBeVisible();
  88  | 
  89  |       // Details sidebar.
  90  |       await expect(page.getByText(/Next due|Próximo cobro/i).last()).toBeVisible();
  91  |       await expect(page.getByText(/Frequency|Frecuencia/i).last()).toBeVisible();
  92  |       await expect(page.getByText(/Source|Origen/i).last()).toBeVisible();
  93  | 
  94  |       // Linked transactions empty state.
  95  |       await expect(page.getByText(/No linked transactions|Aún no hay transacciones/i).last()).toBeVisible();
  96  | 
  97  |       // Action buttons.
  98  |       await expect(page.getByRole("button", { name: /Pause temporarily|Pausar temporalmente/i }).last()).toBeVisible();
  99  |       await expect(page.getByRole("button", { name: /End subscription|Terminar suscripción/i }).last()).toBeVisible();
  100 |       await expect(page.getByRole("button", { name: /Delete permanently|Eliminar permanentemente/i }).last()).toBeVisible();
  101 |     });
  102 | 
  103 |     test("end → reactivate flow on a cancelled series", async ({ page }) => {
  104 |       await page.goto("/recurring/new");
  105 |       if (/\/pricing/.test(page.url())) {
  106 |         test.skip(true, "subscription gate denied");
  107 |         return;
  108 |       }
  109 | 
  110 |       const name = `E2E Restore ${Date.now()}`;
  111 |       const desktopForm = desktopFormShell(page, "recurring-series-form");
  112 |       await desktopForm.locator('input[name="recurring_series[name]"]').fill(name);
  113 |       await desktopForm.locator('input[name="recurring_series[expected_amount]"]').fill("49.00");
  114 |       await Promise.all([
  115 |         page.waitForURL(/\/recurring\/\d+$/, { waitUntil: "domcontentloaded", timeout: 30_000 }),
  116 |         desktopForm.getByRole("button", { name: /Create recurring|Crear recurrente/i }).click(),
  117 |       ]);
  118 | 
  119 |       // End the subscription (desktop copy only).
  120 |       const desktopShow = desktopRecurringShow(page);
  121 |       await Promise.all([
  122 |         page.waitForResponse(
  123 |           (response) => /\/recurring\/\d+/.test(response.url()) && response.ok()
  124 |         ),
  125 |         desktopShow.getByRole("button", { name: /End subscription|Terminar suscripción/i }).click(),
  126 |       ]);
  127 |       await expect(desktopShow.getByText(/Cancelled|Cancelada/i)).toBeVisible();
  128 | 
  129 |       // Reactivate button should now be visible; Pause should not appear at all.
  130 |       const reactivate = desktopShow.getByRole("button", { name: /Reactivate|Reactivar/i });
  131 |       await expect(reactivate).toBeVisible();
  132 |       await expect(desktopShow.getByRole("button", { name: /Pause temporarily|Pausar temporalmente/i })).toHaveCount(0);
  133 | 
  134 |       await reactivate.click();
  135 |       await expect(desktopShow.getByText(/Active|Activa/i)).toBeVisible();
  136 |       await expect(desktopShow.getByRole("button", { name: /Pause temporarily|Pausar temporalmente/i })).toBeVisible();
  137 |     });
  138 |   });
  139 | });
  140 | 
```