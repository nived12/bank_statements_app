<div data-role="category-panel"
     class="fixed z-[9999] w-72 max-h-80 bg-white dark:bg-slate-800 rounded-xl border border-slate-200 dark:border-slate-700 shadow-xl overflow-hidden"
     style="display: none;">
  <!-- Search -->
  <div class="p-2 border-b border-slate-100">
    <%# text-base (16px) prevents iOS Safari auto-zoom on focus %>
    <input type="text"
           data-role="category-search"
           placeholder="<%= t('transactions.inline_edit.search_categories') %>"
           class="w-full px-3 py-1.5 text-base border border-slate-200 dark:border-slate-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none">
  </div>

  <!-- Category list -->
  <div class="overflow-y-auto max-h-64">
    <!-- Clear category option -->
    <button type="button"
            data-category-id=""
            data-category-name="<%= t('transactions.no_category') %>"
            class="w-full text-left px-3 py-2 text-sm text-slate-500 dark:text-slate-400 italic hover:bg-slate-50 dark:hover:bg-slate-700 transition-colors">
      <%= t('transactions.inline_edit.clear_category') %>
    </button>

    <% categories.each do |parent| %>
      <!-- Parent category -->
      <button type="button"
              data-role="category-item"
              data-category-id="<%= parent.id %>"
              data-category-name="<%= parent.name %>"
              class="w-full text-left px-3 py-2 text-sm font-semibold text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-700 transition-colors">
        <div class="flex items-center space-x-2">
          <% if parent.icon.present? %>
            <%= icon_svg(parent.icon, css_classes: "w-4 h-4 text-slate-500 dark:text-slate-400") %>
          <% end %>
          <span><%= parent.name %></span>
        </div>
      </button>

      <!-- Child categories -->
      <% parent.children.each do |child| %>
        <button type="button"
                data-role="category-item"
                data-category-id="<%= child.id %>"
                data-category-name="<%= child.name %>"
                data-category-parent-name="<%= parent.name %>"
                class="w-full text-left pl-8 pr-3 py-1.5 text-sm text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-700 transition-colors">
          <div class="flex items-center space-x-2">
            <% if child.icon.present? %>
              <%= icon_svg(child.icon, css_classes: "w-3.5 h-3.5 text-slate-400") %>
            <% end %>
            <span><%= child.name %></span>
          </div>
        </button>
      <% end %>
    <% end %>
  </div>
</div>
