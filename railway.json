{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'bundle exec rails db:prepare && bundle exec rails db:seed && bundle exec sidekiq -C config/sidekiq.yml & bundle exec rails server -b 0.0.0.0 -p $PORT'"
  }
}
