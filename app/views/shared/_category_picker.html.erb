<%# Locals: categories (Array of parent Category with children loaded),
#           selected_id (Integer|nil),
#           field_name (String, e.g. "transaction[category_id]"),
#           placeholder (String) %>
<div data-controller="multi-select">
  <button type="button"
          data-multi-select-target="button"
          data-action="click->multi-select#toggle"
          class="w-full px-4 py-3 bg-white dark:bg-slate-800 border border-slate-300 dark:border-slate-600 rounded-lg text-left flex items-center justify-between hover:bg-slate-50 dark:hover:bg-slate-700 transition-colors">
    <span data-multi-select-target="summary"
          data-placeholder="<%= placeholder %>"
          class="text-sm <%= selected_id ? 'text-slate-900 dark:text-slate-100' : 'text-slate-400 dark:text-slate-500' %>">
      <% if selected_id %>
        <% selected_category = flatten_categories_hierarchically(categories).find { |cat| cat.id == selected_id } %>
        <%= selected_category&.name || placeholder %>
      <% else %>
        <%= placeholder %>
      <% end %>
    </span>
    <svg class="w-5 h-5 text-slate-400 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
    </svg>
  </button>

  <div data-multi-select-target="container" class="hidden mt-2 border border-slate-300 dark:border-slate-600 rounded-lg bg-white dark:bg-slate-800">
    <div class="flex items-center justify-between p-3 border-b border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-900">
      <span class="text-sm font-medium text-slate-700 dark:text-slate-300"><%= t("transactions.category") %></span>
      <% if selected_id %>
        <button type="button"
                data-action="click->multi-select#clearCategory"
                class="text-sm font-medium text-indigo-600 dark:text-indigo-400 hover:text-indigo-700 dark:hover:text-indigo-300">
          <%= t("transactions.clear_selection", default: "Clear") %>
        </button>
      <% end %>
    </div>

    <div class="border-b border-slate-200 dark:border-slate-700">
      <%# text-base (16px) prevents iOS Safari auto-zoom on focus %>
      <input type="text"
             data-multi-select-target="searchInput"
             data-action="input->multi-select#filterCategories"
             placeholder="<%= t("transactions.search_categories_placeholder") %>"
             class="w-full px-3 py-2 text-base bg-transparent text-slate-900 dark:text-slate-100 placeholder:text-slate-400 dark:placeholder:text-slate-500 focus:outline-none focus:ring-0 border-0"
             autocomplete="off">
    </div>

    <div class="max-h-60 overflow-y-auto p-2">
      <% flatten_categories_hierarchically(categories).each do |category| %>
        <%# Selected classes must match HIGHLIGHT_CLASSES in multi_select_controller.js %>
        <label class="flex items-center p-3 hover:bg-slate-50 dark:hover:bg-slate-700 rounded-lg cursor-pointer <%= 'pl-10' if category.parent_id.present? %> <%= 'bg-indigo-50 dark:bg-indigo-900/30 border border-indigo-200 dark:border-indigo-800' if selected_id == category.id %>"
               data-multi-select-target="categoryLabel"
               data-category-id="<%= category.id %>"
               data-category-parent-id="<%= category.parent_id %>">
          <input type="radio"
                 name="<%= field_name %>"
                 value="<%= category.id %>"
                 <%= "checked" if selected_id == category.id %>
                 class="hidden"
                 data-multi-select-target="radioButton"
                 data-action="change->multi-select#selectCategory"
                 data-category-name="<%= category.name %>">
          <% if category.icon.present? %>
            <div class="w-8 h-8 rounded-full bg-indigo-50 dark:bg-indigo-900/30 flex items-center justify-center mr-3">
              <%= icon_svg(category.icon, css_classes: "w-4 h-4 text-indigo-600 dark:text-indigo-400") %>
            </div>
          <% end %>
          <span data-multi-select-name class="text-sm <%= category.parent_id.nil? ? 'font-semibold text-slate-700 dark:text-slate-200' : 'text-slate-900 dark:text-slate-100' %>"><%= category.name %></span>
        </label>
      <% end %>
    </div>
  </div>
</div>
