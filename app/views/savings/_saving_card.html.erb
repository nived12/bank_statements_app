<div id="saving-<%= saving.id %>"
     class="group bg-white rounded-xl border border-slate-200 hover:border-blue-300 hover:shadow-lg transition-all duration-200 relative">
  <!-- Clickable Card Content -->
  <%= link_to saving_path(saving), class: "block p-6", data: { turbo_prefetch: false } do %>
    <div class="flex items-start justify-between mb-4">
      <!-- Icon and Name -->
      <div class="flex items-center space-x-4 flex-1 min-w-0">
        <div class="w-14 h-14 rounded-xl flex items-center justify-center flex-shrink-0 text-3xl"
             style="background-color: <%= saving.color %>20">
          <%= icon_svg(saving.icon, css_classes: "w-6 h-6") %>
        </div>
        <div class="flex-1 min-w-0">
          <h3 class="text-xl font-semibold text-slate-900 truncate"><%= saving.name %></h3>
          <div class="flex items-center space-x-2 mt-1">
            <span class="text-sm px-2 py-0.5 rounded-full bg-green-100 text-green-800">
              <%= t('savings.savings_goal') %>
            </span>
            <span class="text-sm px-2 py-0.5 rounded-full <%=
              case saving.status
              when 'active' then 'bg-blue-100 text-blue-800'
              when 'completed' then 'bg-green-100 text-green-800'
              when 'paused' then 'bg-slate-100 text-slate-800'
              when 'archived' then 'bg-slate-100 text-slate-800'
              end
            %>">
              <%= t("savings.statuses.#{saving.status}") %>
            </span>
          </div>
        </div>
      </div>
    </div>

    <!-- Progress Bar -->
    <div class="mb-4">
      <div class="flex justify-between text-sm text-slate-600 mb-2">
        <span><%= number_to_currency(saving.current_amount) %></span>
        <span><%= number_to_currency(saving.target_amount) %></span>
      </div>
      <div class="w-full bg-slate-200 rounded-full h-3 overflow-hidden">
        <div class="h-full rounded-full transition-all duration-500"
             style="width: <%= [saving.progress_percentage, 100].min %>%; background-color: <%= saving.color %>"
             title="<%= saving.progress_percentage %>%">
        </div>
      </div>
      <div class="mt-1 text-sm text-slate-600 text-center">
        <%= saving.progress_percentage %>% <%= t('savings.complete').downcase %>
      </div>
    </div>

    <!-- Saving Info -->
    <div class="text-sm text-slate-600 space-y-1">
      <div class="flex justify-between">
        <span><%= t('savings.target_amount') %>:</span>
        <span class="font-medium"><%= number_to_currency(saving.target_amount) %></span>
      </div>
      <div class="flex justify-between">
        <span><%= t('savings.remaining') %>:</span>
        <span class="font-medium"><%= number_to_currency(saving.amount_remaining) %></span>
      </div>
      <% if saving.goals.any? %>
        <div class="flex justify-between">
          <span><%= t('savings.associated_goals') %>:</span>
          <span class="font-medium"><%= saving.goals.count %></span>
        </div>
      <% end %>
    </div>
  <% end %>

  <!-- Action Buttons (positioned absolutely) -->
  <div class="absolute top-4 right-4 flex space-x-1 opacity-0 group-hover:opacity-100 transition-opacity">
    <%= link_to edit_saving_path(saving), class: "p-2 bg-white rounded-lg border border-slate-300 hover:bg-slate-50 transition-colors shadow-sm", data: { turbo_prefetch: false } do %>
      <svg class="w-4 h-4 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
      </svg>
    <% end %>
    <%= button_to saving_path(saving), method: :delete,
                  data: { turbo_confirm: t('savings.delete_confirm', name: saving.name) },
                  class: "p-2 bg-white rounded-lg border border-slate-300 hover:bg-red-50 transition-colors shadow-sm" do %>
      <svg class="w-4 h-4 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
      </svg>
    <% end %>
  </div>
</div>
