<% if @category.parent_id.present? %>
  <%# Editing a subcategory - use subcategory modal %>
  <%= render 'subcategory_modal', category: @category %>
<% else %>
  <%# Editing a parent category - use regular category form %>
  <%= render 'category_form', category: @category %>
<% end %>
