<aside class="hidden pt-20 md:flex md:w-56 md:flex-col md:inset-y-0 z-50 min-w-56">
  <div class="flex flex-col flex-grow pt-5 pb-4 overflow-y-auto">

    <!-- Navigation -->
    <nav class="mt-8 flex-1 space-y-1">

      <!-- Main Navigation -->
      <a href="<%= dashboard_path %>"
          class="sidebar-nav-item group flex items-center px-2 py-2 text-sm font-medium  <%= 'active' if controller_name == 'dashboard' %>">
        <%= icon("layout-dashboard", library: "lucide", variant: "outline", class: "sidebar-nav-icon mr-3 h-5 w-5") %>
        <%= t('navigation.dashboard') %>
      </a>

      <a href="<%= transactions_path %>"
          class="sidebar-nav-item group flex items-center px-2 py-2 text-sm font-medium <%= 'active' if controller_name == 'transactions' %>">
        <%= icon("receipt", library: "lucide", variant: "outline", class: "sidebar-nav-icon mr-3 h-5 w-5") %>
        <%= t('navigation.transactions') %>
      </a>

      <a href="<%= statement_files_path %>"
          class="sidebar-nav-item group flex items-center px-2 py-2 text-sm font-medium <%= 'active' if controller_name == 'statement_files' %>">
        <%= icon("file-text", library: "lucide", variant: "outline", class: "sidebar-nav-icon mr-3 h-5 w-5") %>
        <%= t('navigation.statement_files') %>
      </a>

      <!-- Goals Section -->
      <div class="pt-6">
        <div class="px-2 mb-2">
          <h3 class="text-xs font-semibold text-slate-500 uppercase tracking-wider"><%= t('navigation.goals') %></h3>
        </div>

        <a href="<%= goals_path %>"
            class="sidebar-nav-item group flex items-center px-2 py-2 text-sm font-medium <%= 'active' if controller_name == 'goals' %>">
          <%= icon("target", library: "lucide", variant: "outline", class: "sidebar-nav-icon mr-3 h-5 w-5") %>
          <%= t('navigation.goals') %>
        </a>

        <a href="<%= savings_path %>"
            class="sidebar-nav-item group flex items-center px-2 py-2 text-sm font-medium <%= 'active' if controller_name == 'savings' %>">
          <%= icon("piggy-bank", library: "lucide", variant: "outline", class: "sidebar-nav-icon mr-3 h-5 w-5") %>
          <%= t('navigation.savings') %>
        </a>

        <a href="<%= debts_path %>"
            class="sidebar-nav-item group flex items-center px-2 py-2 text-sm font-medium <%= 'active' if controller_name == 'debts' %>">
          <%= icon("wallet", library: "lucide", variant: "outline", class: "sidebar-nav-icon mr-3 h-5 w-5") %>
          <%= t('navigation.debts') %>
        </a>
      </div>

      <div class="pt-6">
        <div class="px-2 mb-2">
          <h3 class="text-xs font-semibold text-slate-500 uppercase tracking-wider"><%= t('navigation.goals') %></h3>
        </div>

        <a href="<%= categories_path %>"
            class="sidebar-nav-item group flex items-center px-2 py-2 text-sm font-medium <%= 'active' if controller_name == 'categories' %>">
          <%= icon("folder", library: "lucide", variant: "outline", class: "sidebar-nav-icon mr-3 h-5 w-5") %>
          <%= t('navigation.categories') %>
        </a>

        <a href="<%= bank_accounts_path %>"
          class="sidebar-nav-item group flex items-center px-2 py-2 text-sm font-medium <%= 'active' if controller_name == 'bank_accounts' %>">
        <%= icon("credit-card", library: "lucide", variant: "outline", class: "sidebar-nav-icon mr-3 h-5 w-5") %>
        <%= t('navigation.bank_accounts') %>
      </a>
      </div>
    </nav>

    <!-- User Section -->
    <% if current_user %>
      <div class="flex-shrink-0 flex border-t border-slate-200 p-4">
        <div class="flex items-center w-full">
          <img src="<%= current_user.avatar_url %>?t=<%= Time.current.to_i %>"
                alt="<%= current_user.full_name %>"
                class="w-8 h-8 rounded-full object-cover">
          <div class="ml-3 flex-1">
            <p class="text-sm font-medium text-slate-900 truncate"><%= current_user.full_name %></p>
            <div class="flex items-center space-x-2">
              <%= render 'shared/language_switcher' %>
              <form action="/session" method="post" class="inline">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <input type="hidden" name="_method" value="delete" />
                <button type="submit" class="p-1 text-slate-500 hover:text-slate-700 transition-colors" title="<%= t('session.sign_out') %>">
                  <%= icon("log-out", library: "lucide", variant: "outline", class: "w-4 h-4") %>
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</aside>