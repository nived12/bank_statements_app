<!-- TRANSACTIONS PAGE: Transaction Management -->
<main class="transactions-page">
  <div class="transactions-main">
    
    <!-- HEADER SECTION: Page Title and Actions -->
    <header class="transactions-header">
      <div class="transactions-header-actions">
        <div class="transactions-header-left">
          <h1 class="transactions-header-title"><%= t('navigation.transactions') %></h1>
          <p class="transactions-header-subtitle"><%= t('transactions.description') %></p>
        </div>
        <div class="transactions-header-right">
          <%= link_to new_statement_file_path, class: "transactions-button transactions-button-primary" do %>
            <svg class="transactions-button-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"></path>
            </svg>
            <%= t('quick_actions.upload_statement') %>
          <% end %>
        </div>
      </div>
    </header>

    <!-- FILTER SECTION: Inline Filter Bar -->
    <section class="transactions-filters">
      <!-- ACTIVE FILTERS CHIPS -->
      <% total_filters_count = [params[:bank_account_id], params[:statement_file_id], params[:transaction_type], params[:from_date], params[:to_date]].compact.length %>
      <% if total_filters_count > 0 %>
        <div class="flex flex-wrap items-center gap-2 mb-4">
          <span class="text-sm font-medium text-slate-700">Filtros activos:</span>
          <% if params[:bank_account_id].present? %>
            <% bank_account = @bank_accounts.find { |ba| ba.id.to_s == params[:bank_account_id] } %>
            <div class="inline-flex items-center bg-blue-100 text-blue-800 text-sm px-3 py-1 rounded-full">
              <span><%= bank_account&.bank_display_name %> - ••••<%= bank_account&.account_number&.last(4) %></span>
              <button onclick="clearFilter('bank_account_id')" class="ml-2 text-blue-600 hover:text-blue-800">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
              </button>
            </div>
          <% end %>

          <% if params[:statement_file_id].present? %>
            <div class="inline-flex items-center bg-green-100 text-green-800 text-sm px-3 py-1 rounded-full">
              <span><%= @statement_file&.safe_filename %></span>
              <button onclick="clearFilter('statement_file_id')" class="ml-2 text-green-600 hover:text-green-800">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
              </button>
            </div>
          <% end %>

          <% if params[:transaction_type].present? %>
            <div class="inline-flex items-center bg-purple-100 text-purple-800 text-sm px-3 py-1 rounded-full">
              <span><%= t("transaction_types.#{params[:transaction_type]}") %></span>
              <button onclick="clearFilter('transaction_type')" class="ml-2 text-purple-600 hover:text-purple-800">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
              </button>
            </div>
          <% end %>

          <% if params[:from_date].present? || params[:to_date].present? %>
            <div class="inline-flex items-center bg-orange-100 text-orange-800 text-sm px-3 py-1 rounded-full">
              <span>
                <% if params[:from_date].present? && params[:to_date].present? %>
                  <%= params[:from_date] %> - <%= params[:to_date] %>
                <% elsif params[:from_date].present? %>
                  Desde <%= params[:from_date] %>
                <% else %>
                  Hasta <%= params[:to_date] %>
                <% end %>
              </span>
              <button onclick="clearDateFilter()" class="ml-2 text-orange-600 hover:text-orange-800">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
              </button>
            </div>
          <% end %>


          <% if total_filters_count > 0 %>
            <button onclick="clearAllFilters()" class="text-sm text-red-600 hover:text-red-800 font-medium">
              <svg class="w-4 h-4 inline mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
              </svg>
              Limpiar todos
            </button>
          <% end %>
        </div>
      <% end %>

      <!-- SEARCH BAR -->
      <div class="mb-4">
        <div class="max-w-md">
          <label for="search" class="block text-xs font-medium text-slate-600 mb-1"><%= t('transactions.search_description') %></label>
          <div class="relative">
            <input type="text" 
                   id="search" 
                   name="search" 
                   value="<%= params[:search] %>"
                   placeholder="<%= t('transactions.search_placeholder') %>"
                   class="w-full px-3 py-2 pl-10 border border-slate-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                   onkeyup="handleSearchInput()">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <svg class="h-4 w-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
              </svg>
            </div>
            <% if params[:search].present? %>
              <button onclick="clearSearch()" class="absolute inset-y-0 right-0 pr-3 flex items-center text-slate-400 hover:text-slate-600">
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
              </button>
            <% end %>
          </div>
        </div>
      </div>

      <!-- INLINE FILTER BAR -->
      <div class="flex items-center space-x-4 mb-6">
        <!-- BANK ACCOUNT FILTER -->
        <div class="flex-1 min-w-[200px]">
          <label for="bank_account_id" class="block text-xs font-medium text-slate-600 mb-1"><%= t('transactions.bank_account_filter') %></label>
          <select name="bank_account_id" id="bank_account_id" class="w-full px-3 py-2 border border-slate-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent" onchange="applyFilters()">
            <option value=""><%= t('transactions.all_accounts') %></option>
            <% @bank_accounts.each do |ba| %>
              <option value="<%= ba.id %>" <%= "selected" if params[:bank_account_id].to_s == ba.id.to_s %>>
                <%= ba.bank_display_name %> - ••••<%= ba.account_number.last(4) %>
              </option>
            <% end %>
          </select>
        </div>

        <!-- STATEMENT FILE FILTER -->
        <div class="flex-1 min-w-[200px]">
          <label for="statement_file_id" class="block text-xs font-medium text-slate-600 mb-1"><%= t('transactions.statement_file_filter') %></label>
          <select name="statement_file_id" id="statement_file_id" class="w-full px-3 py-2 border border-slate-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent" onchange="applyFilters()">
            <option value=""><%= t('transactions.all_statement_files') %></option>
            <% @statement_files.each do |sf| %>
              <option value="<%= sf.id %>" <%= "selected" if params[:statement_file_id].to_s == sf.id.to_s %>>
                <%= sf.safe_filename %> - <%= sf.bank_account.display_name %>
              </option>
            <% end %>
          </select>
        </div>

        <!-- TRANSACTION TYPE FILTER -->
        <div class="flex-1 min-w-[150px]">
          <label for="transaction_type" class="block text-xs font-medium text-slate-600 mb-1"><%= t('transactions.transaction_type_filter') %></label>
          <select name="transaction_type" id="transaction_type" class="w-full px-3 py-2 border border-slate-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent" onchange="applyFilters()">
            <option value=""><%= t('transactions.all_transaction_types') %></option>
            <option value="income" <%= "selected" if params[:transaction_type] == "income" %>><%= t('transaction_types.income') %></option>
            <option value="fixed_expense" <%= "selected" if params[:transaction_type] == "fixed_expense" %>><%= t('transaction_types.fixed_expense') %></option>
            <option value="variable_expense" <%= "selected" if params[:transaction_type] == "variable_expense" %>><%= t('transaction_types.variable_expense') %></option>
          </select>
        </div>

        <!-- DATE RANGE PICKER -->
        <div class="flex-1 min-w-[200px]">
          <label for="date_range" class="block text-xs font-medium text-slate-600 mb-1"><%= t('transactions.date_range_filter') %></label>
          <input type="text" 
                 id="date_range" 
                 name="date_range" 
                 placeholder="<%= t('transactions.click_to_select_range') %>"
                 class="w-full px-3 py-2 border border-slate-300 rounded-md text-sm cursor-pointer focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                 readonly>
          <input type="hidden" name="from_date" id="from_date">
          <input type="hidden" name="to_date" id="to_date">
        </div>
      </div>

    </section>

    <!-- STATS OVERVIEW: Transaction Statistics -->
    <section class="transactions-stats">
      
      <!-- TOTAL TRANSACTIONS STAT -->
      <div class="transactions-stat-card">
        <div class="transactions-stat-content">
          <div class="transactions-stat-icon transactions-stat-icon-total">
            <svg class="transactions-stat-icon-svg transactions-stat-icon-svg-total" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2zm0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
            </svg>
          </div>
          <div>
            <p class="transactions-stat-label"><%= t('table_headers.transactions') %></p>
            <p class="transactions-stat-value transactions-stat-value-total"><%= @pagy.count %></p>
          </div>
        </div>
      </div>

      <!-- INCOME STAT -->
      <div class="transactions-stat-card">
        <div class="transactions-stat-content">
          <div class="transactions-stat-icon transactions-stat-icon-income">
            <svg class="transactions-stat-icon-svg transactions-stat-icon-svg-income" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
            </svg>
          </div>
          <div>
            <p class="transactions-stat-label"><%= t('transaction_types.income') %></p>
            <p class="transactions-stat-value transactions-stat-value-income">
              <%= number_to_currency(@filtered_transactions.where(transaction_type: 'income').sum(:amount), unit: "$") %>
            </p>
          </div>
        </div>
      </div>

      <!-- EXPENSES STAT -->
      <div class="transactions-stat-card">
        <div class="transactions-stat-content">
          <div class="transactions-stat-icon transactions-stat-icon-expenses">
            <svg class="transactions-stat-icon-svg transactions-stat-icon-svg-expenses" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 9V7a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2m2 4h10a2 2 0 002-2v-6a2 2 0 00-2-2H9a2 2 0 00-2 2v6a2 2 0 002 2zm7-5a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
          </div>
          <div>
            <p class="transactions-stat-label"><%= t('transaction_types.expenses') %></p>
            <p class="transactions-stat-value transactions-stat-value-expenses">
              <%= number_to_currency(@filtered_transactions.where(transaction_type: ['fixed_expense', 'variable_expense']).sum(:amount), unit: "$") %>
            </p>
          </div>
        </div>
      </div>

      <!-- CATEGORIES STAT -->
      <div class="transactions-stat-card">
        <div class="transactions-stat-content">
          <div class="transactions-stat-icon transactions-stat-icon-total">
            <svg class="transactions-stat-icon-svg transactions-stat-icon-svg-total" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
            </svg>
          </div>
          <div>
            <p class="transactions-stat-label"><%= t('navigation.categories') %></p>
            <p class="transactions-stat-value transactions-stat-value-total">
              <%= @filtered_transactions.joins(:category).distinct.count(:category_id) %>
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- TRANSACTIONS TABLE: Transaction List -->
    <% if @transactions.any? %>
      <section class="transactions-table-container">
        <div class="overflow-x-auto">
          <table class="transactions-table">
            <thead class="transactions-table-header">
              <tr>
                <th class="transactions-table-header-row">
                  <span class="text-slate-700 font-medium">#</span>
                </th>
                <th class="transactions-table-header-row">
                  <a class="transactions-table-header-sortable" href="/transactions?sort=date&direction=<%= @current_sort == 'date' && @current_direction == 'desc' ? 'asc' : 'desc' %>&bank_account_id=<%= params[:bank_account_id] %>&statement_file_id=<%= params[:statement_file_id] %>&transaction_type=<%= params[:transaction_type] %>&from_date=<%= params[:from_date] %>&to_date=<%= params[:to_date] %>&search=<%= params[:search] %>&page=<%= params[:page] %>" onclick="preserveScrollPosition()">
                    <%= t('table_headers.date') %>
                    <%= render_sort_indicator('date') %>
                  </a>
                </th>
                <th class="transactions-table-header-row">
                  <a class="transactions-table-header-sortable" href="/transactions?sort=description&direction=<%= @current_sort == 'description' && @current_direction == 'desc' ? 'asc' : 'desc' %>&bank_account_id=<%= params[:bank_account_id] %>&statement_file_id=<%= params[:statement_file_id] %>&transaction_type=<%= params[:transaction_type] %>&from_date=<%= params[:from_date] %>&to_date=<%= params[:to_date] %>&search=<%= params[:search] %>&page=<%= params[:page] %>" onclick="preserveScrollPosition()">
                    <%= t('table_headers.description') %>
                    <%= render_sort_indicator('description') %>
                  </a>
                </th>
                <th class="transactions-table-header-row transactions-table-header-row-right">
                  <a class="transactions-table-header-sortable" href="/transactions?sort=amount&direction=<%= @current_sort == 'amount' && @current_direction == 'desc' ? 'asc' : 'desc' %>&bank_account_id=<%= params[:bank_account_id] %>&statement_file_id=<%= params[:statement_file_id] %>&transaction_type=<%= params[:transaction_type] %>&from_date=<%= params[:from_date] %>&to_date=<%= params[:to_date] %>&search=<%= params[:search] %>&page=<%= params[:page] %>" onclick="preserveScrollPosition()">
                    <%= t('table_headers.amount') %>
                    <%= render_sort_indicator('amount') %>
                  </a>
                </th>
                <th class="transactions-table-header-row">
                  <a class="transactions-table-header-sortable" href="/transactions?sort=transaction_type&direction=<%= @current_sort == 'transaction_type' && @current_direction == 'desc' ? 'asc' : 'desc' %>&bank_account_id=<%= params[:bank_account_id] %>&statement_file_id=<%= params[:statement_file_id] %>&transaction_type=<%= params[:transaction_type] %>&from_date=<%= params[:from_date] %>&to_date=<%= params[:to_date] %>&search=<%= params[:search] %>&page=<%= params[:page] %>" onclick="preserveScrollPosition()">
                    <%= t('table_headers.transaction_type') %>
                    <%= render_sort_indicator('transaction_type') %>
                  </a>
                </th>
                <th class="transactions-table-header-row">
                  <a class="transactions-table-header-sortable" href="/transactions?sort=category&direction=<%= @current_sort == 'category' && @current_direction == 'desc' ? 'asc' : 'desc' %>&bank_account_id=<%= params[:bank_account_id] %>&statement_file_id=<%= params[:statement_file_id] %>&transaction_type=<%= params[:transaction_type] %>&from_date=<%= params[:from_date] %>&to_date=<%= params[:to_date] %>&search=<%= params[:search] %>&page=<%= params[:page] %>" onclick="preserveScrollPosition()">
                    <%= t('table_headers.category') %>
                    <%= render_sort_indicator('category') %>
                  </a>
                </th>
                <th class="transactions-table-header-row"><%= t('table_headers.actions') %></th>
              </tr>
            </thead>
            <tbody class="transactions-table-body" id="transactions-tbody">
              <% @transactions.each.with_index(1) do |transaction, index| %>
                <tr class="transactions-table-row">
                  <td class="transactions-table-cell">
                    <span class="transactions-table-cell-text text-slate-500 font-medium">
                      <%= index %>
                    </span>
                  </td>
                  <td class="transactions-table-cell">
                    <span class="transactions-table-cell-text">
                      <%= l(transaction.date, format: :transaction) %>
                    </span>
                  </td>
                  <td class="transactions-table-cell">
                    <div>
                      <div class="transactions-table-cell-text" title="<%= transaction.description %>">
                        <%= short_description(transaction.description) %>
                      </div>
                      <% if transaction.bank_account %>
                        <div class="transactions-table-cell-account">
                          <%= transaction.bank_account.bank_display_name %> - ••••<%= transaction.bank_account.account_number.last(4) %>
                        </div>
                      <% end %>
                    </div>
                  </td>
                  <td class="transactions-table-cell transactions-table-cell-amount">
                    <span class="<%= transaction.transaction_type == 'income' ? 'transactions-table-cell-amount-credit' : 'transactions-table-cell-amount-debit' %>">
                      <%= format_currency(transaction.amount) %>
                    </span>
                  </td>
                  <td class="transactions-table-cell">
                    <span class="transactions-category">
                      <%= transaction.transaction_type.humanize %>
                    </span>
                  </td>
                  <td class="transactions-table-cell">
                    <% if transaction.category %>
                      <div class="flex flex-col">
                        <% if transaction.category.parent %>
                          <!-- Parent category -->
                          <span class="transactions-category transactions-category-parent">
                            <%= transaction.category.parent.name %>
                          </span>
                          <!-- Child category -->
                          <span class="transactions-category transactions-category-child">
                            <%= transaction.category.name %>
                          </span>
                        <% else %>
                          <!-- Parent category only (no subcategory) -->
                          <span class="transactions-category transactions-category-parent">
                            <%= transaction.category.name %>
                          </span>
                        <% end %>
                      </div>
                    <% else %>
                      <span class="transactions-category-empty"><%= t('transactions.no_category') %></span>
                    <% end %>
                  </td>
                  <td class="transactions-table-cell">
                    <div class="transactions-actions">
                      <button onclick="openEditModal('<%= transaction.id %>')" class="transactions-action-btn transactions-action-btn-edit" title="<%= t('transactions.edit_transaction') %>">
                        <svg class="transactions-action-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                        </svg>
                      </button>
                      <% if transaction.statement_file %>
                        <div class="relative group">
                          <%= link_to statement_file_path(transaction.statement_file), class: "block" do %>
                            <div class="w-6 h-6 bg-blue-100 rounded-full flex items-center justify-center cursor-pointer hover:bg-blue-200 transition-colors duration-200">
                              <svg class="w-4 h-4 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                              </svg>
                            </div>
                          <% end %>
                          <!-- Tooltip -->
                          <div class="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 px-3 py-2 bg-slate-900 text-white text-xs rounded-lg opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none whitespace-nowrap z-10">
                            <div class="text-center">
                              <div class="font-medium mb-1"><%= t('transactions.statement_file') %></div>
                              <div class="text-slate-200"><%= transaction.statement_file.file.filename.to_s %></div>
                              <div class="text-slate-300 text-xs mt-1"><%= t('statement_files.click_to_view_details') %></div>
                            </div>
                            <!-- Tooltip arrow -->
                            <div class="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-slate-900"></div>
                          </div>
                        </div>
                      <% end %>
                    </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
          
          <!-- Visible element for intersection observer -->
          <div id="scroll-trigger" class="h-4 w-full bg-transparent"></div>
          
          <!-- Pagination Indicator -->
          <div class="mt-6 text-center" 
               id="infinite-scroll-container"
               data-next-page="<%= @pagy.next || (@pagy.page < @pagy.pages ? @pagy.page + 1 : '') %>"
               data-current-page="<%= @pagy.page %>"
               data-total-pages="<%= @pagy.pages %>"
               data-total-count="<%= @pagy.count %>">
            <div class="text-sm text-slate-600 mb-2" id="pagination-info">
              <%= t('transactions.showing_page') %> <%= @pagy.page %> <%= t('transactions.of') %> <%= @pagy.pages %> 
              (<%= t('transactions.loaded') %>: <span id="transactions-loaded-count"><%= @transactions.count %></span> / <%= t('monthly_summary.total_transactions') %>: <%= @pagy.count %>)
            </div>
            
            <!-- Loading indicator for infinite scroll -->
            <div id="infinite-scroll-indicator" class="hidden">
              <div class="inline-flex items-center px-4 py-2 text-sm text-slate-600">
                <svg class="animate-spin h-4 w-4 mr-2" xmlns="http://www/w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                  <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                  <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                </svg>
                <%= t('transactions.loading') %>
              </div>
            </div>
          </div>
        </div>
      </section>
    <% else %>
      
      <!-- EMPTY STATE: No Transactions Yet -->
      <section class="transactions-empty">
        <div class="w-24 h-24 bg-slate-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <svg class="w-12 h-12 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2zm0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
        </div>
        <h3 class="transactions-empty-title"><%= t('empty_states.no_transactions_yet') %></h3>
        <p class="transactions-empty-description"><%= t('bank_accounts.upload_statement_help') %></p>
        <%= link_to new_statement_file_path, class: "transactions-empty-action" do %>
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"></path>
          </svg>
          <%= t('quick_actions.upload_statement') %>
        <% end %>
      </section>
    <% end %>
  </div>
</main>

<!-- EDIT TRANSACTION MODAL: Transaction Edit Form -->
<div id="editModal" class="fixed inset-0 bg-slate-600 bg-opacity-50 overflow-y-auto h-full w-full hidden z-50">
  <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
    <div class="mt-3">
      <h3 class="text-lg font-medium text-slate-900 mb-4"><%= t('transactions.edit_transaction') %></h3>
      <form id="editForm" method="post" class="space-y-4">
        <input type="hidden" name="_method" value="patch">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-2"><%= t('transactions.merchant') %></label>
          <input type="text" name="transaction[merchant]" id="edit_merchant" class="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
        </div>
        
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-2"><%= t('transactions.reference') %></label>
          <input type="text" name="transaction[reference]" id="edit_reference" class="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
        </div>
        
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-2"><%= t('transactions.transaction_type') %></label>
          <select name="transaction[transaction_type]" id="edit_transaction_type" class="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            <% Transaction.transaction_types.keys.each do |opt| %>
              <option value="<%= opt %>"><%= opt.humanize %></option>
            <% end %>
          </select>
        </div>
        
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-2"><%= t('table_headers.category') %></label>
          <select name="transaction[category_id]" id="edit_category_id" class="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            <option value=""><%= t('transactions.no_category') %></option>
            <% current_user.categories.order(:name).each do |cat| %>
              <option value="<%= cat.id %>"><%= cat.name %></option>
            <% end %>
          </select>
        </div>
        
        <div class="flex justify-end space-x-3 pt-4">
          <button type="button" onclick="closeEditModal()" class="px-4 py-2 border border-slate-300 text-sm font-medium rounded-lg text-slate-700 bg-white hover:bg-slate-50 transition-colors duration-200">
            <%= t('form_actions.cancel') %>
          </button>
          <button type="submit" class="px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition-colors duration-200">
            <%= t('form_actions.save_changes') %>
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
function openEditModal(transactionId) {
  // Set the form action
  document.getElementById('editForm').action = `<%= root_path %>transactions/${transactionId}`;
  
  // Show the modal
  document.getElementById('editModal').classList.remove('hidden');
}

function closeEditModal() {
  document.getElementById('editModal').classList.add('hidden');
}

// Close modal when clicking outside
document.getElementById('editModal').addEventListener('click', function(e) {
  if (e.target === this) {
    closeEditModal();
  }
});

// Infinite Scrolling for Transactions
let isLoading = false;
let currentObserver = null;

function setupInfiniteScroll() {
  console.log("Setting up infinite scroll...");
  
  const container = document.getElementById('infinite-scroll-container');
  if (!container) {
    console.log("No infinite scroll container found");
    return;
  }
  
  const nextPage = parseInt(container.dataset.nextPage);
  const totalPages = parseInt(container.dataset.totalPages);
  
  if (!nextPage || nextPage > totalPages) {
    console.log("No next page available");
    return;
  }
  
  console.log(`Setting up infinite scroll for page ${nextPage} of ${totalPages}`);
  
  // Clean up existing observer if any
  if (currentObserver) {
    currentObserver.disconnect();
  }
  
  // Set up intersection observer for infinite scrolling
  currentObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting && !isLoading) {
        // Check if we still have more pages before triggering
        const container = document.getElementById('infinite-scroll-container');
        if (container) {
          const nextPage = parseInt(container.dataset.nextPage);
          const totalPages = parseInt(container.dataset.totalPages);
          
          if (nextPage && nextPage <= totalPages) {
            console.log("Scroll trigger visible, loading more transactions...");
            loadMoreTransactions();
          } else {
            console.log("No more pages available, not triggering load");
          }
        }
      }
    });
  }, {
    rootMargin: '100px' // Start loading 100px before reaching the trigger
  });

  // Observe the scroll trigger element
  const scrollTrigger = document.getElementById('scroll-trigger');
  if (scrollTrigger) {
    currentObserver.observe(scrollTrigger);
    console.log("Observer set up for scroll trigger");
  } else {
    console.log("No scroll trigger found");
  }
}

function loadMoreTransactions() {
  if (isLoading) {
    console.log("Already loading, skipping request");
    return;
  }
  
  const container = document.getElementById('infinite-scroll-container');
  if (!container) return;
  
  const nextPage = parseInt(container.dataset.nextPage);
  const totalPages = parseInt(container.dataset.totalPages);
  
  if (!nextPage || nextPage > totalPages) {
    console.log("No next page available, stopping infinite scroll");
    // Hide the scroll trigger when no more pages
    const scrollTrigger = document.getElementById('scroll-trigger');
    if (scrollTrigger) {
      scrollTrigger.style.display = 'none';
    }
    return;
  }
  
  console.log(`Loading page ${nextPage}`);
  isLoading = true;
  
  // Show loading state
  const indicator = document.getElementById('infinite-scroll-indicator');
  if (indicator) {
    indicator.classList.remove('hidden');
  }

  // Build URL with current filters and sorting
  const url = new URL(window.location);
  url.searchParams.set('page', nextPage);
  
  // Preserve all current filter parameters
  const currentParams = new URLSearchParams(window.location.search);
  ['bank_account_id', 'statement_file_id', 'transaction_type', 'from_date', 'to_date', 'search', 'sort', 'direction'].forEach(param => {
    if (currentParams.has(param)) {
      url.searchParams.set(param, currentParams.get(param));
    }
  });
  
  fetch(url, {
    headers: {
      'X-Requested-With': 'XMLHttpRequest',
      'Accept': 'text/html'
    }
  })
  .then(response => {
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    return response.text();
  })
  .then(html => {
    console.log("Received response, length:", html.length);
    
    // Append new rows to the table
    const tbody = document.getElementById('transactions-tbody');
    if (tbody) {
      tbody.insertAdjacentHTML('beforeend', html);
    }
    
    // Update container state
    container.dataset.currentPage = nextPage.toString();
    
    // Check if there are more pages
    if (nextPage >= totalPages) {
      console.log("No more pages, hiding scroll trigger");
      // Hide the scroll trigger when no more pages
      const scrollTrigger = document.getElementById('scroll-trigger');
      if (scrollTrigger) {
        scrollTrigger.style.display = 'none';
      }
      // Also disconnect the observer
      if (currentObserver) {
        currentObserver.disconnect();
        currentObserver = null;
      }
    } else {
      // Update next page for next load
      const newNextPage = nextPage + 1;
      container.dataset.nextPage = newNextPage.toString();
      console.log(`Updated next page to ${newNextPage}`);
    }
    
    // Update pagination info
    updatePaginationInfo(nextPage);
  })
  .catch(error => {
    console.error('Error loading more transactions:', error);
  })
  .finally(() => {
    // Hide loading indicator
    if (indicator) {
      indicator.classList.add('hidden');
    }
    isLoading = false;
  });
}

function updatePaginationInfo(currentPage) {
  const container = document.getElementById('infinite-scroll-container');
  const totalPages = parseInt(container.dataset.totalPages);
  const totalCount = parseInt(container.dataset.totalCount);
  
  // Count actual loaded transactions
  const tbody = document.getElementById('transactions-tbody');
  const loadedCount = tbody ? tbody.querySelectorAll('tr').length : 0;
  
  console.log(`Updating pagination: currentPage=${currentPage}, totalPages=${totalPages}, loadedCount=${loadedCount}`);
  
  const paginationInfo = document.getElementById('pagination-info');
  if (paginationInfo) {
    paginationInfo.innerHTML = `
      Mostrando página ${currentPage} de ${totalPages} 
      (Transacciones cargadas: ${loadedCount} / Total de transacciones: ${totalCount})
    `;
  }
  
  // Also update the container's current page data attribute
  container.dataset.currentPage = currentPage.toString();
}

// Initialize infinite scrolling when the page loads
// This works for both direct navigation and Turbo Drive navigation
function initializeInfiniteScroll() {
  // Clean up any existing setup first
  if (currentObserver) {
    currentObserver.disconnect();
    currentObserver = null;
  }
  isLoading = false;
  
  // Set up the infinite scroll
  setupInfiniteScroll();
}

// Preserve scroll position when sorting
function preserveScrollPosition() {
  // Store current scroll position before navigation
  sessionStorage.setItem('transactionsScrollPosition', window.scrollY);
}

// Restore scroll position after page load
function restoreScrollPosition() {
  const savedPosition = sessionStorage.getItem('transactionsScrollPosition');
  if (savedPosition) {
    // Use requestAnimationFrame for smoother scrolling
    requestAnimationFrame(() => {
      window.scrollTo({
        top: parseInt(savedPosition),
        behavior: 'smooth'
      });
      // Clear the saved position after restoring
      sessionStorage.removeItem('transactionsScrollPosition');
    });
  }
}

document.addEventListener('DOMContentLoaded', initializeInfiniteScroll);
document.addEventListener('DOMContentLoaded', restoreScrollPosition);

// Also listen for Turbo Drive events to handle navigation
document.addEventListener('turbo:load', initializeInfiniteScroll);
document.addEventListener('turbo:load', restoreScrollPosition);
document.addEventListener('turbo:render', initializeInfiniteScroll);
document.addEventListener('turbo:render', restoreScrollPosition);

// Fallback: if none of the above events fire, try to set up after a short delay
setTimeout(initializeInfiniteScroll, 100);
setTimeout(restoreScrollPosition, 100);

// Initialize date range picker
function initializeDateRangePicker() {
  const dateRangeInput = document.getElementById('date_range');
  const fromDateInput = document.getElementById('from_date');
  const toDateInput = document.getElementById('to_date');
  
  if (!dateRangeInput) return;
  
  // Wait for flatpickr to be available
  if (typeof window.flatpickr === 'undefined') {
    console.log('Waiting for flatpickr to load...');
    setTimeout(initializeDateRangePicker, 100);
    return;
  }
  
  // Set initial values if they exist in the URL
  const urlParams = new URLSearchParams(window.location.search);
  const fromDate = urlParams.get('from_date');
  const toDate = urlParams.get('to_date');
  
  if (fromDate && toDate) {
    fromDateInput.value = fromDate;
    toDateInput.value = toDate;
    dateRangeInput.value = `${fromDate} - ${toDate}`;
  } else if (fromDate) {
    fromDateInput.value = fromDate;
    dateRangeInput.value = `Desde ${fromDate}`;
  } else if (toDate) {
    toDateInput.value = toDate;
    dateRangeInput.value = `Hasta ${toDate}`;
  }
  
  // Initialize flatpickr
  const picker = window.flatpickr(dateRangeInput, {
    mode: "range",
    dateFormat: "Y-m-d",
    maxDate: "today",
    allowInput: false,
    clickOpens: true,
    defaultDate: fromDate && toDate ? [fromDate, toDate] : (fromDate ? fromDate : (toDate ? toDate : null)),
    onChange: function(selectedDates, dateStr, instance) {
      if (selectedDates.length === 2) {
        const startDate = selectedDates[0];
        const endDate = selectedDates[1];
        
        // Update hidden inputs
        fromDateInput.value = startDate.toISOString().split('T')[0];
        toDateInput.value = endDate.toISOString().split('T')[0];
        
        // Update display
        dateRangeInput.value = `${startDate.toISOString().split('T')[0]} - ${endDate.toISOString().split('T')[0]}`;
        
        // Auto-submit the form
        applyFilters();
      }
    },
    onClear: function() {
      fromDateInput.value = '';
      toDateInput.value = '';
      dateRangeInput.value = '';
      // Auto-submit when cleared
      applyFilters();
    }
  });
  
}


// Initialize date range picker when page loads
document.addEventListener('DOMContentLoaded', initializeDateRangePicker);
document.addEventListener('turbo:load', initializeDateRangePicker);
document.addEventListener('turbo:render', initializeDateRangePicker);

// Inline filter functionality
function applyFilters() {
  const bankAccountId = document.getElementById('bank_account_id').value;
  const statementFileId = document.getElementById('statement_file_id').value;
  const transactionType = document.getElementById('transaction_type').value;
  const fromDate = document.getElementById('from_date').value;
  const toDate = document.getElementById('to_date').value;
  const search = document.getElementById('search').value;
  
  // Build URL with current filters
  const url = new URL(window.location);
  
  // Set or clear parameters
  if (bankAccountId) {
    url.searchParams.set('bank_account_id', bankAccountId);
  } else {
    url.searchParams.delete('bank_account_id');
  }
  
  if (statementFileId) {
    url.searchParams.set('statement_file_id', statementFileId);
  } else {
    url.searchParams.delete('statement_file_id');
  }
  
  if (transactionType) {
    url.searchParams.set('transaction_type', transactionType);
  } else {
    url.searchParams.delete('transaction_type');
  }
  
  if (fromDate) {
    url.searchParams.set('from_date', fromDate);
  } else {
    url.searchParams.delete('from_date');
  }
  
  if (toDate) {
    url.searchParams.set('to_date', toDate);
  } else {
    url.searchParams.delete('to_date');
  }
  
  if (search) {
    url.searchParams.set('search', search);
  } else {
    url.searchParams.delete('search');
  }
  
  // Preserve sorting parameters
  const currentParams = new URLSearchParams(window.location.search);
  ['sort', 'direction'].forEach(param => {
    if (currentParams.has(param)) {
      url.searchParams.set(param, currentParams.get(param));
    }
  });
  
  // Navigate to the new URL
  window.location.href = url.toString();
}

// Cascading dropdown functionality
function updateStatementFiles() {
  const bankAccountId = document.getElementById('bank_account_id').value;
  const statementFileSelect = document.getElementById('statement_file_id');
  
  // Clear current options except the first one
  statementFileSelect.innerHTML = '<option value=""><%= t('transactions.all_statement_files') %></option>';
  
  if (bankAccountId) {
    // Fetch statement files for the selected bank account
    fetch(`/transactions/statement_files?bank_account_id=${bankAccountId}`, {
      headers: {
        'X-Requested-With': 'XMLHttpRequest',
        'Accept': 'application/json'
      }
    })
    .then(response => response.json())
    .then(data => {
      data.statement_files.forEach(file => {
        const option = document.createElement('option');
        option.value = file.id;
        option.textContent = `${file.safe_filename} - ${file.bank_account_display_name}`;
        statementFileSelect.appendChild(option);
      });
    })
    .catch(error => {
      console.error('Error fetching statement files:', error);
    });
  } else {
    // Load all statement files
    fetch('/transactions/statement_files', {
      headers: {
        'X-Requested-With': 'XMLHttpRequest',
        'Accept': 'application/json'
      }
    })
    .then(response => response.json())
    .then(data => {
      data.statement_files.forEach(file => {
        const option = document.createElement('option');
        option.value = file.id;
        option.textContent = `${file.safe_filename} - ${file.bank_account_display_name}`;
        statementFileSelect.appendChild(option);
      });
    })
    .catch(error => {
      console.error('Error fetching statement files:', error);
    });
  }
}

// Add event listener for bank account changes
document.addEventListener('DOMContentLoaded', function() {
  const bankAccountSelect = document.getElementById('bank_account_id');
  if (bankAccountSelect) {
    bankAccountSelect.addEventListener('change', function() {
      updateStatementFiles();
      // Clear statement file selection when bank account changes
      document.getElementById('statement_file_id').value = '';
      applyFilters();
    });
  }
});

function clearFilter(filterName) {
  const element = document.getElementById(filterName);
  if (element) {
    element.value = '';
    applyFilters();
  }
}

function clearDateFilter() {
  document.getElementById('from_date').value = '';
  document.getElementById('to_date').value = '';
  document.getElementById('date_range').value = '';
  applyFilters();
}

// Search functionality
let searchTimeout;

function handleSearchInput() {
  // Clear existing timeout
  clearTimeout(searchTimeout);
  
  // Set a new timeout to search after user stops typing (500ms delay)
  searchTimeout = setTimeout(() => {
    applyFilters();
  }, 500);
}

function clearSearch() {
  document.getElementById('search').value = '';
  applyFilters();
}

function clearAllFilters() {
  // Clear all filter inputs
  document.getElementById('bank_account_id').value = '';
  document.getElementById('statement_file_id').value = '';
  document.getElementById('transaction_type').value = '';
  document.getElementById('from_date').value = '';
  document.getElementById('to_date').value = '';
  document.getElementById('date_range').value = '';
  document.getElementById('search').value = '';
  
  // Navigate to clean transactions page
  window.location.href = '/transactions';
}
</script>
