<div data-controller="transactions-edit">

  <!-- Header section -->
  <%= render 'common/section_header', title: t('transactions.new_transaction'), back_path: transactions_path %>

  <!-- Form -->
  <div class="w-full flex justify-center">
    <div class="container p-8">
      <%= render 'form', transaction: Transaction.new, bank_accounts: @bank_accounts %>
    </div>
  </div>
</div>