<% if @category.parent_id.present? || params[:parent_id].present? %>
  <%# Creating a subcategory %>
  <%= render 'subcategory_modal', category: @category %>
<% else %>
  <%# Creating a parent category %>
  <%= render 'category_form', category: @category %>
<% end %>
