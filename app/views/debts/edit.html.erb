<div data-controller="debt-edit">

  <!-- Header section -->
  <%= render 'common/section_header', title: t('debts.edit_debt'), back_path: debts_path %>

  <!-- Form -->
  <div class="w-full flex justify-center">
    <div class="container p-8">
      <%= render 'form', debt: @debt %>
    </div>
  </div>
</div>